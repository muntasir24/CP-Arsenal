//vector pair
        vector<pair<int,int>> vp(n);
for (int i = 0; i < n; ++i)
        {
          cin>>vp[i].second;
        
        }
        for (int i = 0; i < n; ++i)
        {
          cin>>x;
          vp[i].first=abs(x);
        }
